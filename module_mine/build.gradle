apply from:"../module.build.gradle"

android {
    defaultConfig {
        if (isRunAlone.toBoolean()){
            applicationId "com.example.mine"
        }
    }
    resourcePrefix "mine_"
    namespace 'com.example.module_usercenter'
}
